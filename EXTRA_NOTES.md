Tips:
- Use `git log --oneline --graph --decorate --all` to visualize history.
- To force push after rebase: `git push -f origin feature/your-branch`
- To recover after a mistake: `git reflog` then `git checkout -b recover <hash>`
